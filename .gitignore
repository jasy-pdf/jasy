/dist
node_modules/
packages/**/dist
/coverage

# Claude's private scratch area — tools, scripts, generated PDFs. Not part of the project.
/claude-data

# Shared working plan between Flo and Claude. Not part of the published package.
/todo.md

# Manual DX/showcase harness — depends on local (gitignored) sample assets, so it isn't
# self-contained for a fresh clone. To be reintroduced as a polished public example later.
/tests/manual

# Playground scratch output — the source (play.ts) is a committed example, the rendered PDFs aren't.
packages/playground/*.pdf
packages/playground/*.png
# npm pack / release tarballs
*.tgz
